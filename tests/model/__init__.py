import sys
from pathlib import Path

sys.path.append(Path.cwd() / ".." / ".." / "model")
# sys.path.append(Path.cwd() / "model" / "db")
sys.path.append(Path.cwd() / "util")
# sys.path.append(Path.cwd() / "view")
